const initialState = {
    Cart: [],
    selectAddon: {},
    totalPrice: 0,
};

const CartReducer = (state = initialState, { type, payload }) => {
    switch (type) {
        default:
            return state;
    }
};

export default CartReducer;